# User Command Execution Rule

## Important: User Executes Only System/Execution Commands

**User Responsibility:** Execute only commands that involve system operations, processes, or execution

### User Should Execute:
- **System Commands:**
  - `cargo check` - Compile verification
  - `cargo build` - Build the project
  - `cargo run` - Run the application
  - `cargo test` - Run tests
  - `cargo add <package>` - Add dependencies

- **Process Management:**
  - `taskkill` - Kill processes
  - `netstat` - Network statistics
  - `Get-Process` - Process information
  - `Stop-Process` - Stop processes
  - `Start-Process` - Start processes

- **Server Operations:**
  - `cargo run --release` - Production build
  - `cargo install` - Install binaries
  - `sqlx migrate run` - Run database migrations
  - `sqlx migrate revert` - Revert migrations

- **Development Tools:**
  - `rustup update` - Update Rust toolchain
  - `cargo update` - Update dependencies
  - `cargo clean` - Clean build artifacts

### AI Can Handle:
- ✅ **File Creation/Modification:**
  - Creating new Rust files
  - Updating existing code
  - Writing structs, functions, modules
  - Adding imports and dependencies

- ✅ **Configuration Files:**
  - Updating `Cargo.toml`
  - Creating `.env` files
  - Writing configuration code
  - Setting up module declarations

- ✅ **Documentation:**
  - Updating README files
  - Adding comments to code
  - Creating documentation
  - Updating migration checklists

- ✅ **Project Structure:**
  - Creating directories
  - Setting up module structure
  - Organizing code files
  - Managing project layout

### Examples:

**User Executes:**
```powershell
cargo check
cargo run
taskkill /F /IM rust.exe
```

**AI Handles:**
```powershell
# AI provides these commands for user to run
New-Item -Path "src\models\runs.rs" -ItemType File -Force
Set-Content -Path "src\models\runs.rs" -Value "// Rust code here"
```

### Workflow:
1. **AI provides PowerShell commands** for file operations
2. **User runs execution commands** like `cargo check`, `cargo run`
3. **AI handles all code changes** and file updates
4. **User only executes** system-level commands

### Never Ask User To:
- ❌ Write code manually
- ❌ Create files manually
- ❌ Update README manually
- ❌ Modify configuration files manually

### Always Provide:
- ✅ Complete PowerShell commands for file operations
- ✅ Ready-to-run code snippets
- ✅ Clear instructions for execution commands